<%= form_for([@page, @comment]) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "コメントを入力(140文字以内)"%>
  </div>
  <%= f.submit "送信", class: "btn btn-primary" %>
<% end %>
